function removeChar(str){
    // input: string
    // output: Return the first and last chars of my string
    
      // Declear a veriable set to empty string 
    let outpuut = "";
      // Use the sustring method to get the first and last of the chars indexes 
    let results = str.substring(1, str.length -1);
    // Return the string results
      return results;
    };
    