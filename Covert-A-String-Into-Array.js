// Input: a empty string
//Output: String converted to a array
function stringToArray(string) {
  // Set output variable to an empty string
  let output = "";
  // Used a string as a sperator for each word
  let arr = string.split(" ");
  // Return that array as the new aray
  return arr;
}
