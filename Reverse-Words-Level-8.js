//input: string
//output: string
//reverses all of the words in the string 
// Use the .split(), .reverse(), and .join() methods
function reverseWords(str){
    //return string
  return str.split(' ').reverse().join(' ')
  }
  